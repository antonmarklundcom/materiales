# Config y datos de runtime viven en el servidor, fuera del repo (ver DEPLOY.md — quedan
# no-públicos por .htaccess, no por estar fuera del docroot: el Git de Hostinger en hosting
# compartido despliega dentro de public_html).
/config/
/storage/

# Ruido de sistema
.DS_Store
Thumbs.db
*.log

# Capturas locales de Playwright (fases 9+): nunca se commitean.
/docs/screenshots/

# tests/mobile-overflow.mjs (fase 14) usa Playwright: si alguien lo instala local con
# `npm i -D playwright` en vez de correr contra la instalación global, no se commitea.
/node_modules/
/package-lock.json

# Worktrees de subagentes de Claude Code (locales, nunca se commitean).
/.claude/worktrees/

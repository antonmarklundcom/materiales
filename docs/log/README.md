# docs/log — one file per phase (plan §4.14)

`<phase-id>-<slug>.md`, ≤ 12 lines "Built", ≤ 8 "Decisions", ≤ 8 "Known issues", one
"Verification" line. `plan.md` §9 holds one index line per phase; the detail lives here.
Phases 1–8 predate this rule: their entries are inline in `plan.md` §9.
